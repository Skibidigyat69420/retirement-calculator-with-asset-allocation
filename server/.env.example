PORT=4000
NODE_ENV=development
LOG_LEVEL=info
DATABASE_URL=
SEED_DEMO_WORKSPACE=false

# Optional. When configured, /auth/session and onboarding verify the Supabase user
# from the bearer token using auth.getUser().
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
